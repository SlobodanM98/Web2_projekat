import { NominatimResponse } from './nominatim-response.model';

describe('NominatimResponse', () => {
  it('should create an instance', () => {
    expect(new NominatimResponse()).toBeTruthy();
  });
});
